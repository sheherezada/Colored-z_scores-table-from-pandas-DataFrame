1. Make imports :to create or open DataFrame , from scipy.stats import zscore: to take the z_scores from the 
columns

2.Create or open the DataFrame

3.Create a new DataFrame with the z_scores

4. Creates two lists one with min values of each column, second with with the max values

5.Apply color function and display as table

When running the file in jupyter notebook or elsewhere the table will be colored!
